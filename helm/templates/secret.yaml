{{- if and .Values.secrets.database_url (ne .Values.secrets.database_url "") }}
apiVersion: v1
kind: Secret
metadata:
  name: ip-reverser-secrets
  namespace: {{ .Release.Namespace }}
type: Opaque
data:
  database-url: {{ .Values.secrets.database_url | b64enc }}
{{- end }}